# Dependencias del repositorio — Equipo 2 (Horno fusor, control moderno)
# Instalar con:  pip install -r requirements.txt
#
# Las versiones son mínimos sugeridos (>=). Si necesitas reproducibilidad
# estricta, fija las versiones exactas con == usando las que tengas instaladas.

numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.7
torch>=2.0
